// Entrypoint para rodar a app localmente (NPM START)
import ReactDOM from "react-dom";
import "antd/dist/antd.css";

import App from "./app";

ReactDOM.render(<App />, document.getElementById("app"));
